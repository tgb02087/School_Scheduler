<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
    </style>

    <style name="ToolbarTitleText" > <!-- 툴바 설정 -->
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#232323</item>
    </style>

    <style name="ButtonSetting" > <!-- 버튼 설정 -->
        <item name="android:textSize">18dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">@color/colorPrimaryDark</item>   <!-- 배경색 지정-->
    </style>

    <style name="addButton" > <!-- 버튼 설정 -->
        <item name="android:textSize">18dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ffffff</item>
        <!--<item name="android:background">@color/colorPrimaryDark</item>-->   <!-- 배경색 지정-->
    </style>

    <style name="TextViewSetting" > <!--  설정 -->
        <item name="android:textSize">20dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#6F495C</item>
    </style>

    <style name="TextViewMain" > <!--  설정 -->
        <item name="android:textSize">15dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#6F495C</item>
    </style>

    <style name="TextViewSchedule" > <!-- 텍스트 설정 -->
        <item name="android:textSize">15dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:textColor">#6F495C</item>
    </style>

</resources>
